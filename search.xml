<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>HTML-C1</title>
    <url>/2024/03/10/HTML-C1/</url>
    <content><![CDATA[<h1 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h1><ul>
<li>_<strong>元素</strong>_：开始标签、内容、结束标签三部分组成的整体</li>
<li>_<strong>属性</strong>_：是写在一个元素的开始标签中的特殊词汇，它们用于控制这个元素的行为</li>
</ul>
<h2 id="基本结构"><a href="#基本结构" class="headerlink" title="基本结构"></a>基本结构</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;en&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;styles.css&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;utf-8&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;viewport&quot;</span> <span class="attr">content</span>=<span class="string">&quot;width=device-width, initial-scale=1.0&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span> <span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">main</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">section</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">h1</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">article</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">section</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">main</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h1 id="常用标签及属性"><a href="#常用标签及属性" class="headerlink" title="常用标签及属性"></a>常用标签及属性</h1><h2 id="“规范性“标签"><a href="#“规范性“标签" class="headerlink" title="“规范性“标签"></a>“规范性“标签</h2><h3 id=""><a href="#" class="headerlink" title="&lt;!DOCTYPE html&gt;"></a><code>&lt;!DOCTYPE html&gt;</code></h3><p>所有页面都应以其开头，被称为<em>声明</em>，确保浏览器尝试满足行业范围的规范</p>
<h2 id="“功能性”标签"><a href="#“功能性”标签" class="headerlink" title="“功能性”标签"></a>“功能性”标签</h2><h3 id="img"><a href="#img" class="headerlink" title="img"></a><em><code>img</code></em></h3><ul>
<li>注意它是<strong>自闭合</strong>标签（无结束标签）</li>
<li>常用属性：<code>alt</code>（替代文本）、<code>src</code></li>
</ul>
<h3 id="a"><a href="#a" class="headerlink" title="a"></a><em><code>a</code></em></h3><p>_<strong>锚点元素（超链接）</strong>_，链接到另一个页面</p>
<ul>
<li>常用属性：<code>href</code> 、<ul>
<li><code>target</code>  规定在何处打开链接<ul>
<li><code>_blank</code>  在新窗口中打开</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="ul-ol"><a href="#ul-ol" class="headerlink" title="ul &amp; ol"></a><em><code>ul</code> &amp; <code>ol</code></em></h3><p><strong><em>无序&amp;有序列表</em></strong></p>
<h3 id="li"><a href="#li" class="headerlink" title="li"></a><em><code>li</code></em></h3><p><strong><em>列表项</em></strong></p>
<h3 id="form"><a href="#form" class="headerlink" title="form"></a><em><code>form</code></em></h3><p><em>表单</em></p>
<ul>
<li>常用属性：<code>action</code> （指示应将表单数据发送到哪里）<ul>
<li><code>method</code>  规定如何发送表单数据<ul>
<li>常用参数：<ul>
<li>get：<strong>将表单数据以名称&#x2F;值对的形式附加到 URL 中</strong>，对于用户希望将结果加为书签的表单提交很有用，<strong>更适合非安全数据</strong></li>
<li>post：<strong>在 HTTP 请求的正文中附加表单数据</strong>（数据不会显示在 URL 中）<strong>，适合敏感数据</strong></li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="input"><a href="#input" class="headerlink" title="input"></a><em><code>input</code></em></h3><ul>
<li>注意它是<strong>自闭合的、内联的</strong></li>
<li>常用属性：<code>placeholder</code> ，<code>required</code> （注意该属性无属性值，代表“必填”），<code>checked</code>（注意该属性无属性值，代表“使该复选框或单选按钮默认被选中”）<ul>
<li><code>type</code>  创建多种输入<ul>
<li>text：文本，表单数据将为输入的内容</li>
<li>radio：单选，表单数据将为<code>name</code> &#x2F; <code>value</code> 属性对</li>
<li>checkbox：复选框，表单数据将为<code>name</code> &#x2F; <code>value</code> 属性对</li>
</ul>
</li>
<li><code>name</code>  指定输入数据的存放位置，或者说指明数据”属性“<ul>
<li>可以用于限制单选，通过<strong>给每一个单选按钮添加相同的  <code>name</code>  属性以关联单选按钮</strong></li>
</ul>
</li>
<li><code>value</code>  设置 input 的数据值<ul>
<li>常在<code>type=radio</code>时使用</li>
</ul>
</li>
</ul>
</li>
<li>距离父级  <code>form</code>  元素最近的第一个  <code>type</code>  为  <code>submit</code>  的  <code>input</code>  元素会被当作提交按钮<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;submit&quot;</span> <span class="attr">value</span>=<span class="string">&quot;Submit&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="label"><a href="#label" class="headerlink" title="label"></a><em><code>label</code></em></h3><p>帮助将  <code>input</code>  、<code>select</code>等元素的文本与元素本身关联起来，<strong>默认是行内元素</strong></p>
<ul>
<li>用法<ol>
<li>套在<code>input</code>外面</li>
<li>套在<code>input</code>的文本外面，并添加与  <code>input</code> 的  <code>id</code> 具有相同值的  <code>for</code> 属性</li>
</ol>
</li>
</ul>
<h3 id="button"><a href="#button" class="headerlink" title="button"></a><em><code>button</code></em></h3><p>单击没有任何属性的表单按钮的默认行为会将数据提交到表单的  <code>action</code>  属性中指定的位置</p>
<ul>
<li>注意它是<strong>内联的</strong></li>
<li>常用属性：<code>type</code></li>
</ul>
<h3 id="select"><a href="#select" class="headerlink" title="select"></a><em><code>select</code></em></h3><p>给表单添加一个下拉列表</p>
<p><code>select</code>  是包含一组  <code>option</code>  元素的容器，<code>option</code>  元素就是下拉列表的内容</p>
<h3 id="option"><a href="#option" class="headerlink" title="option"></a><em><code>option</code></em></h3><ul>
<li>每一个  <code>option</code>  需要指定一个  <code>value</code>  属性。 如果没有指定，向服务器默认提交的是  <code>option</code>  内的文本</li>
</ul>
<h3 id="textarea"><a href="#textarea" class="headerlink" title="textarea"></a><em><code>textarea</code></em></h3><ul>
<li>和  <code>text</code>  类型的  <code>input</code>  类似，区别是 <code>textarea</code> 有一些额外的好处，比如可以方便地添加更多的文本以及设置默认显示的行数和列宽。</li>
</ul>
<h3 id="与"><a href="#与" class="headerlink" title="&lt;span&gt;  与 &lt;div&gt;"></a><code>&lt;span&gt;</code>  与 <code>&lt;div&gt;</code></h3><ul>
<li><code>&lt;div&gt;</code>是块级元素，而  <code>&lt;span&gt;</code>  是行内元素</li>
</ul>
<h2 id="“美化性”标签"><a href="#“美化性”标签" class="headerlink" title="“美化性”标签"></a>“美化性”标签</h2><ul>
<li><em><code>main</code>  正文</em></li>
<li><em><code>footer</code>  页脚</em></li>
<li><em><code>article</code> 信息内容</em></li>
<li><em><code>div</code>  用于设计布局</em><ul>
<li>常用属性：<code>class</code></li>
</ul>
</li>
<li><em><code>em</code>斜体</em></li>
<li><em><code>strong</code>  加粗</em></li>
<li><em><code>figure</code>独立引用内容</em><ul>
<li><em><code>figcaption</code>： 说明</em></li>
</ul>
</li>
<li>_<code>fieldset</code>  块级元素_（划分内容）<ul>
<li><em><code>legend</code> ：<code>fieldset</code></em> 元素中内容的<em>标题</em></li>
</ul>
</li>
<li><em><code>hr</code>  分隔符</em><ul>
<li>注意是<strong>自闭合标签</strong></li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>OS-C1</title>
    <url>/2024/03/11/OS-C1/</url>
    <content><![CDATA[<ul>
<li>1983 年图灵奖 Dennis Ritchie 和 Ken Thompson：Unix 操作系统的开发与实现、C 语言的发明人</li>
<li>嵌入式系统：一个功能完备、几乎不依赖其他外部装置即可独立运行的软硬件集成的系统，有自己独立的 OS</li>
</ul>
<h1 id="什么是操作系统"><a href="#什么是操作系统" class="headerlink" title="什么是操作系统"></a>什么是操作系统</h1><h2 id="操作系统在计算机系统中的层次"><a href="#操作系统在计算机系统中的层次" class="headerlink" title="操作系统在计算机系统中的层次"></a><strong>操作系统在计算机系统中的层次</strong></h2><p><img src="https://s2.loli.net/2024/03/12/Sx6RCUaQsFy7izB.png" alt="Untitled" loading="lazy" onerror='this.onerror=null;this.src="/img/404.jpg"'></p>
<p><img src="https://s2.loli.net/2024/03/12/2r1oyCUpljnEhqW.png" alt="Untitled" loading="lazy" onerror='this.onerror=null;this.src="/img/404.jpg"'></p>
<ul>
<li>自顶向下看——管理硬件：<strong>屏蔽复杂性-抽象</strong></li>
<li>自底向上看——<strong>提供服务（构筑生态）：资源的复用-共享</strong></li>
</ul>
<h2 id="操作系统的定义与作用"><a href="#操作系统的定义与作用" class="headerlink" title="操作系统的定义与作用"></a>操作系统的定义与作用</h2><p>是一组<strong>管理计算机硬件资源</strong>的软件集合</p>
<ul>
<li>用户与计算机硬件系统之间的接口（API&#x2F;GUI）</li>
<li>系统资源的管理者（处理机、存储器、I&#x2F;O 设备等）</li>
<li>实现对计算机资源的抽象（OS 是扩充机&#x2F;虚拟机）</li>
</ul>
<h2 id="计算机体系结构中的“接口”"><a href="#计算机体系结构中的“接口”" class="headerlink" title="计算机体系结构中的“接口”"></a><strong>计算机体系结构中的“接口”</strong></h2><h3 id="API"><a href="#API" class="headerlink" title="API"></a>API</h3><p>Application <strong>Programming</strong> Interface，是应用程序编程接口，目的是提供应用程序与开发人员基于某软件或硬件得以访问一组例程的能力</p>
<ul>
<li>在不同平台的具体实现方式可能不同，但是<strong>接口的形式和功能必须一致</strong>，才能实现跨平台开发</li>
</ul>
<h3 id="ABI"><a href="#ABI" class="headerlink" title="ABI"></a>ABI</h3><p>Application <strong>Binary</strong> Interface，应用程序和操作系统之间，一个应用和它的库之间，或者应用的组成部分之间的二进制层面的底层接口</p>
<ul>
<li>反映<strong>二进制可执行代码兼容性</strong></li>
</ul>
<h3 id="API-VS-ABI"><a href="#API-VS-ABI" class="headerlink" title="API VS ABI"></a>API VS ABI</h3><ul>
<li>描述的内容不同</li>
<li>作用的层面不同。<strong>API</strong>描述<strong>代码级层面</strong>的接口，<strong>ABI</strong>描述<strong>二进制层面</strong>的接口</li>
<li>兼容的难度不同。<strong>ABI 的兼容程度比 API 更为严格</strong></li>
</ul>
<h1 id="操作系统简史"><a href="#操作系统简史" class="headerlink" title="操作系统简史"></a>操作系统简史</h1><p><img src="https://s2.loli.net/2024/03/12/MIarNyD9icUJpGq.png" alt="Untitled" loading="lazy" onerror='this.onerror=null;this.src="/img/404.jpg"'></p>
<h2 id="spectre（幽灵）-meltdown-（熔断）CPU-漏洞"><a href="#spectre（幽灵）-meltdown-（熔断）CPU-漏洞" class="headerlink" title="spectre（幽灵） &amp; meltdown （熔断）CPU 漏洞"></a>spectre（幽灵） &amp; meltdown （熔断）<strong>CPU 漏洞</strong></h2><p>利用分支预测错误的 speculative execution（CPU 的缓存机制和乱序执行），访问到不该访问的信息，并且同样是通过 cache 这个 side channel 传递出去</p>
<h2 id="批处理系统"><a href="#批处理系统" class="headerlink" title="批处理系统"></a>批处理系统</h2><p>加载在计算机上的一个系统软件，使得计算机能自动地、成批地处理一个或多个用户的作业</p>
<p>▪ 两个发展阶段：联机和脱机</p>
<p>▪ 联机：作业的输入&#x2F;输出由 CPU 处理</p>
<ul>
<li>关键部分：监督程序、磁带</li>
<li>缺点：CPU”忙等“时间长（等待慢速的输入&#x2F;输出设备完成工作），利用率低</li>
</ul>
<p>▪ <strong>脱机</strong>：输入&#x2F;输出<strong>脱离主机控制</strong>，<strong>缓解高速主机与慢速外设的矛盾</strong>，提高 CPU 利用率</p>
<ul>
<li>缺点：若每次主机内存中仅存放一道作业（单道程序系统），CPU 空闲时间长（单道运行期间发出输入&#x2F;输出（I&#x2F;O）请求后）<ul>
<li>解决方案：<strong>多道程序系统</strong>（允许多个程序同时进入内存并运行）<ul>
<li>缺点：平均周转时间长，<strong>不能提供交互作用能力</strong><ul>
<li>解决方案：分时系统</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><img src="https://s2.loli.net/2024/03/12/p1scX9Vl4mvKxFW.png" alt="联机批处理系统" loading="lazy" onerror='this.onerror=null;this.src="/img/404.jpg"'></p>
<p>联机批处理系统</p>
<p><img src="https://s2.loli.net/2024/03/12/7DgNmzUcGCiM9W6.png" alt="脱机批处理系统" loading="lazy" onerror='this.onerror=null;this.src="/img/404.jpg"'></p>
<p>脱机批处理系统</p>
<p><img src="https://s2.loli.net/2024/03/12/6HKD7FWRBImZNds.png" alt="分时系统" loading="lazy" onerror='this.onerror=null;this.src="/img/404.jpg"'></p>
<p>分时系统</p>
<h2 id="分时系统"><a href="#分时系统" class="headerlink" title="分时系统"></a>分时系统</h2><p>把处理机的运行时间分成很短的时间片，<strong>按时间片轮流把处理机分配</strong>给各联机作业使用</p>
<ul>
<li>特点：<ul>
<li>改变了程序访问硬件的方式<ul>
<li>应用软件不能直接访问物理内存，因为物理内存是所有软件共享的 → <strong>页式内存管理(Paging)、虚拟内存</strong><br><img src="https://s2.loli.net/2024/03/12/7DBIq4j1EpU3Nnv.png" alt="Untitled" loading="lazy" onerror='this.onerror=null;this.src="/img/404.jpg"'></li>
</ul>
</li>
<li>交互性：用户可根据系统对请求的响应结果，进一步向系统提出新的请求</li>
</ul>
</li>
<li>相关概念：<strong>可重入</strong>——程序或子程序可以<strong>安全的被并行执行</strong></li>
<li>提高响应速度的方法：采用可重入代码、引入虚存减少对换</li>
<li>和多道程序系统的区别：分时操作系统是给<strong>不同用户</strong>提供程序的使用，而多道程序系统是<strong>不同程序</strong>间的穿插运行</li>
</ul>
<h2 id="System-360"><a href="#System-360" class="headerlink" title="System&#x2F;360"></a><strong>System&#x2F;360</strong></h2><ul>
<li>背景：厂家和用户需要软件在不同型号的计算机之间兼容</li>
<li><strong>特点：</strong>不同型号的计算机将能享用同样的设备，但更大的困难却是在软件方面，存在很多缺陷</li>
</ul>
<h2 id="操作系统的网络化"><a href="#操作系统的网络化" class="headerlink" title="操作系统的网络化"></a><strong>操作系统的网络化</strong></h2><h3 id="Network-Operating-System-网络操作系统"><a href="#Network-Operating-System-网络操作系统" class="headerlink" title="Network Operating System 网络操作系统"></a>Network Operating System 网络操作系统</h3><p>在传统单机 OS 上加单独软件层，主要提供联网功能和资源的远程访问，实现多机互联</p>
<h3 id="Distributed-Operating-System-分布式操作系统"><a href="#Distributed-Operating-System-分布式操作系统" class="headerlink" title="Distributed Operating System 分布式操作系统"></a>Distributed Operating System 分布式操作系统</h3><p>多台机器<strong>统一管理</strong>形成<strong>单一系统</strong>，相比网络操作系统，<strong>对用户和应用高度透明</strong></p>
<h1 id="操作系统的基本实现机制"><a href="#操作系统的基本实现机制" class="headerlink" title="操作系统的基本实现机制"></a><strong>操作系统的基本实现机制</strong></h1><h2 id="异常：trap-interrupt"><a href="#异常：trap-interrupt" class="headerlink" title="异常：trap &amp; interrupt"></a>异常：trap &amp; interrupt</h2><ul>
<li>系统调用<ul>
<li>视作同步异常中的陷阱 trap</li>
<li><strong>window</strong>自陷指令： <strong>int 0x2e</strong></li>
</ul>
</li>
</ul>
<p><img src="https://s2.loli.net/2024/03/12/BPCMUZbHi4SJ6k7.png" alt="Untitled" loading="lazy" onerror='this.onerror=null;this.src="/img/404.jpg"'></p>
<ul>
<li>异步异常是由<strong>外部因素</strong>触发（计算机系统或操作系统自动引发），且<strong>无法通过常规的异常处理</strong>机制捕获</li>
<li>同步异常是由<strong>程序内部的错误</strong>引发（通常由程序员在代码中显式地引发），并且通过<strong>异常处理机制</strong>进行捕获和处理</li>
<li>陷阱是程序有意，可以使执行流程从用户态陷入内核，进而<strong>满足软件访问硬件或者调用内核中函数的需求</strong></li>
</ul>
<h1 id="操作系统的基本类型"><a href="#操作系统的基本类型" class="headerlink" title="操作系统的基本类型"></a><strong>操作系统的基本类型</strong></h1><p>批处理系统、分时系统、实时系统、混合型</p>
<h1 id="操作系统的特征与功能"><a href="#操作系统的特征与功能" class="headerlink" title="操作系统的特征与功能"></a><strong>操作系统的特征与功能</strong></h1><h3 id="特征"><a href="#特征" class="headerlink" title="特征"></a><strong>特征</strong></h3><p>并发、共享、虚拟、异步</p>
<h3 id="功能"><a href="#功能" class="headerlink" title="功能"></a><strong>功能</strong></h3><p>处理机管理（进程）、存储器管理（内存）、设备管理（外设，虚拟设备，IO）、文件管理、作业控制</p>
<h3 id="要解决的基本问题"><a href="#要解决的基本问题" class="headerlink" title="要解决的基本问题"></a>要解决的基本问题</h3><p>解决冲突、协调并发、保证数据一致、实现数据控制</p>
<h1 id="操作系统结构"><a href="#操作系统结构" class="headerlink" title="操作系统结构"></a><strong>操作系统结构</strong></h1><h3 id="模块接口"><a href="#模块接口" class="headerlink" title="模块接口"></a><strong>模块接口</strong></h3><ul>
<li>优点：加速了操作系统的研制过程、增加了操作系统的灵活性、便于修改和维护</li>
<li>缺点：接口定义困难、无序性</li>
</ul>
<h3 id="有序分层法"><a href="#有序分层法" class="headerlink" title="有序分层法"></a><strong>有序分层法</strong></h3><p>层次结构设计，规定严格的调用关系等</p>
<h3 id="虚拟机"><a href="#虚拟机" class="headerlink" title="虚拟机"></a><strong>虚拟机</strong></h3><ul>
<li>本质：把<strong>多道程序</strong>和<strong>扩充机器（更方便的拓展界面）</strong>的功能完全分开</li>
<li>优势：使每一部分都简单、灵活，更易于维护</li>
<li>缺陷：性能开销比较大</li>
</ul>
<h3 id="微内核结构"><a href="#微内核结构" class="headerlink" title="微内核结构"></a><strong>微内核结构</strong></h3><p>内核中只包括中断处理、进程通信、基本调度等（进程相关操作），文件系统、网络功能、内存管理、设备管理等作为服务在微内核上运行。</p>
<ul>
<li>优点：内核易于实现、可移植性好、配置灵活、适应分布式环境（本地内核与远程内核对服务同样的支持）</li>
<li>缺点：速度较慢（扩大内核—减少切换；减少内核—提高其他优点）</li>
</ul>
<h3 id="机制与策略分开"><a href="#机制与策略分开" class="headerlink" title="机制与策略分开"></a><strong>机制与策略分开</strong></h3><p>灵活、可扩展</p>
]]></content>
      <categories>
        <category>OS</category>
      </categories>
      <tags>
        <tag>Notes</tag>
      </tags>
  </entry>
  <entry>
    <title>OS-Lab0</title>
    <url>/2024/03/12/OS-Lab0/</url>
    <content><![CDATA[<h1 id="讲解部分梳理"><a href="#讲解部分梳理" class="headerlink" title="讲解部分梳理"></a>讲解部分梳理</h1><h2 id="Linux基本操作"><a href="#Linux基本操作" class="headerlink" title="Linux基本操作"></a>Linux基本操作</h2><ul>
<li><p><code>head</code> 输出文件首部内容</p>
<ul>
<li><code>-n &lt;n&gt;</code> 显示前 <n> 行内容<br>  <code>-c &lt;n&gt;</code> 显示前 <n> 个字节内容</li>
</ul>
</li>
<li><p><code>tail</code> 输出文件尾部内容</p>
<ul>
<li><p><code>-n &lt;n&gt;</code> 显示末尾 <n> 行内容<br>  <code>-c &lt;n&gt;</code> 显示末尾 <n> 个字节内容</p>
<p>  <code>-f</code> 当文件增长时，输出后续添加的数据（适用于文件不断变化的情况，如日志文件）</p>
</li>
</ul>
</li>
<li><p><code>ps</code> 显示当前进程状态</p>
<ul>
<li><code>-aux / -ef</code>  显示所有进程</li>
</ul>
</li>
<li><p><code>mv</code> 还可以用于重命名</p>
</li>
<li><p>递归参数</p>
<ul>
<li><code>-p</code> <strong>从指定目录向上级目录递归</strong> eg 创建目录</li>
<li><code>-r</code> <strong>从指定目录向其下级目录递归</strong> eg 递归删除、拷贝</li>
</ul>
</li>
<li><p>在命令后加上<code>&amp;</code>符号，代表让命令在后台进行</p>
</li>
<li><p><code>Ctrl Q</code> 进入块选模式，方便删注释和缩进</p>
<ul>
<li>选择之后可以通过 <code>I</code> 进入插入模式，方便段落缩进</li>
<li><code>&#123;</code> <code>&#125;</code> 向上&#x2F;下选中1段</li>
</ul>
</li>
</ul>
<h2 id="Makefile-补充"><a href="#Makefile-补充" class="headerlink" title="Makefile 补充"></a>Makefile 补充</h2><ul>
<li><p>开头最好写<code>.PHONY xx</code> (一般会写<code>.PHONY：clean</code>）</p>
<ul>
<li><code>.PHONY</code>后面跟的目标都被称为伪目标，对应的命令一定会被执行</li>
<li>作用：避免伪目标同名文件冲突；提高执行效率</li>
</ul>
</li>
<li><p>每一个指令 (command) 之前<strong>必须按一次制表符键</strong>来控制间隔，而不能是空格，否则 make 会报错</p>
</li>
<li><p>简洁高效的写法</p>
  <figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用变量维护Makefile</span></span><br><span class="line"><span class="comment"># %.o: %.c 缩小编写量</span></span><br><span class="line">CC = gcc</span><br><span class="line">OBJ = function.o test.o</span><br><span class="line">TARGET = test</span><br><span class="line"></span><br><span class="line"><span class="variable">$(TARGET)</span>: <span class="variable">$(OBJ)</span></span><br><span class="line">		<span class="variable">$(CC)</span> <span class="variable">$(OBJ)</span> -o <span class="variable">$(TARGET)</span></span><br><span class="line"></span><br><span class="line"><span class="section">%.o: %.c</span></span><br><span class="line">		<span class="variable">$(CC)</span> -c <span class="variable">$&lt;</span> -o <span class="variable">$@</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>外层makefile调用内层makefile</p>
  <figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">all：</span><br><span class="line">    <span class="variable">$(MAKE)</span> -C subdir</span><br><span class="line"></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">	<span class="variable">$(MAKE)</span> clean -C subdir</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="Git补充"><a href="#Git补充" class="headerlink" title="Git补充"></a>Git补充</h2><h3 id="git-branch"><a href="#git-branch" class="headerlink" title="git branch"></a><code>git branch</code></h3><p><code>git branch &lt;branch-name&gt;</code> 本地创建一个基于当前分支产生的分支。</p>
<h3 id="git-status"><a href="#git-status" class="headerlink" title="git status"></a><strong><code>git status</code></strong></h3><p><strong>查看当前分支的状态</strong>，以及当前工作区的变动和暂存区的内容</p>
<h3 id="git-add"><a href="#git-add" class="headerlink" title="git add"></a><strong><code>git add</code></strong></h3><p>把一个<strong>新文件</strong>或者已经<strong>修改过的文件</strong>加入<strong>暂存区</strong>中</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add &lt;file_name&gt;         <span class="comment"># 将指定文件加入暂存区</span></span><br><span class="line">git add .                   <span class="comment"># 或使用.将项目所有文件加入暂存区（除.gitignore指定的文件外）</span></span><br></pre></td></tr></table></figure>

<h3 id="git-restore"><a href="#git-restore" class="headerlink" title="git restore"></a><strong><code>git restore</code></strong></h3><p>放弃修改（如果涉及到文件的增删，建议用reset，用的时候慎重）</p>
<ul>
<li><p>当文件还没有通过 <code>git add</code> 加入暂存区时，使用 <code>git restore &lt;filename&gt;</code> 来撤销修改，使其退回到上一个 commit 的状态</p>
</li>
<li><p>如果文件已经加入了暂存区，通过 <code>git restore **--staged** &lt;filename&gt;</code> 来取消暂存。</p>
</li>
<li><p><strong>其他错误回退命令</strong></p>
<ul>
<li>若文件未添加至暂存区：使用 <code>git checkout &lt;filename&gt;</code> 将文件替换成暂存区版本，或 <code>git checkout .</code> 将工作区内的所有文件替换成暂存区内的文件（谨慎使用）</li>
<li>若文件已添加到暂存区但未提交到本地仓库：使用 <code>git reset HEAD .</code> 撤销暂存区的所有修改至工作区，接下来就回到了上一步</li>
<li>若已提交到本地仓库：使用 <code>git reset --hard HRAD^</code> 将所有文件回退至上一版本</li>
</ul>
</li>
</ul>
<h3 id="git-reset"><a href="#git-reset" class="headerlink" title="git reset"></a><strong><code>git reset</code></strong></h3><p><code>git reset --hard</code> 可以<strong>恢复文件的版本</strong>，既可以往旧版本恢复，也可以往新版本“恢复”。但–hard 是 reset 命令唯一的危险用法，使用时如果工作区的文件还未提交，git 会覆盖它从而导致无法恢复。所以使用时要<strong>小心谨慎</strong></p>
<h3 id="git-checkout"><a href="#git-checkout" class="headerlink" title="git checkout"></a><strong><code>git checkout</code></strong></h3><p><strong>切换分支</strong></p>
<p>注意，在切换时，需要<strong>保证目前所有文件的状态均为“未修改”（没有修改过，或者已经提交）</strong></p>
<h3 id="git-commit"><a href="#git-commit" class="headerlink" title="git commit"></a><strong><code>git commit</code></strong></h3><p>使用 <code>git commit -m &lt;message&gt;</code> 这个命令将暂存区的修改提交到储存库中。<strong>当 message 参数有空格时需要把 message 用引号括起来</strong></p>
<h3 id="git-rm"><a href="#git-rm" class="headerlink" title="git rm"></a><code>git rm</code></h3><ul>
<li>加<code>--cached</code> 从暂存区删除，但不删除工作区文件，不加则两边都删</li>
</ul>
<h3 id="删除分支"><a href="#删除分支" class="headerlink" title="删除分支"></a>删除分支</h3><p><code>git branch -d(D) &lt;branch-name&gt;</code> 删除本地对应分支</p>
<p><code>git push origin --delete [branch_name]</code> 删除远程分支</p>
<h3 id="建立孤儿分支"><a href="#建立孤儿分支" class="headerlink" title="建立孤儿分支"></a>建立孤儿分支</h3><ol>
<li><code>git checkout --orphan &lt;branch name&gt;</code> 创建孤立分支</li>
<li><code>git rm -rf .</code>  删除拷贝的所有文件</li>
<li><code>touch add commit</code></li>
<li><code>git push -u origin &lt;branch name&gt;</code> 创建远程分支并将其关联</li>
</ol>
<h3 id="提交空文件夹"><a href="#提交空文件夹" class="headerlink" title="提交空文件夹"></a>提交空文件夹</h3><p>Git 只对跟踪文件进行版本控制，而不跟踪文件夹本身。如果想要提交一个空文件夹到 Git 仓库中，可以<strong>在文件夹中添加一个占位文件</strong>，例如“.gitkeep”</p>
<h3 id="合并分支"><a href="#合并分支" class="headerlink" title="合并分支"></a>合并分支</h3><p><code>git merge</code>  自动合并</p>
<ul>
<li>如果自动合并不成功（存在冲突）<ul>
<li><p>不想继续合并了，退出合并的中间状态</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git merge --abort</span><br></pre></td></tr></table></figure>
</li>
<li><p>处理冲突 <strong>最好在图形化界面上解决，更方便</strong></p>
<ol>
<li><code>git status</code>查看冲突的文件</li>
<li>编辑冲突文件，解决冲突（记得删除三行分隔线）</li>
<li><code>git add 冲突文件</code></li>
<li><code>git commit -m &quot;提交信息&quot;</code></li>
</ol>
</li>
</ul>
</li>
</ul>
<h2 id="文本处理三剑客"><a href="#文本处理三剑客" class="headerlink" title="文本处理三剑客"></a>文本处理三剑客</h2><h3 id="grep-查找"><a href="#grep-查找" class="headerlink" title="grep 查找"></a>grep 查找</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep [选项] PATTERN FIL</span><br><span class="line">选项（常用）：</span><br><span class="line">-a 不忽略二进制数据进行搜索。</span><br><span class="line">-i 忽略大小写差异。</span><br><span class="line">-r 从目录中递归查找。</span><br><span class="line">-n 显示行号</span><br><span class="line">-q 安静模式，不打印结果</span><br></pre></td></tr></table></figure>

<h3 id="sed-替换、删除等"><a href="#sed-替换、删除等" class="headerlink" title="sed 替换、删除等"></a>sed 替换、删除等</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sed [选项] <span class="string">&#x27;命令&#x27;</span> 输入文本</span><br><span class="line">选项（常用）：</span><br><span class="line">-n：安静模式，只显示经过sed处理的内容。否则显示输入文本的所有内容。</span><br><span class="line">**-i：直接修改读取的档案内容，而不是输出到屏幕。否则，只输出不编辑。</span><br><span class="line">-E 选项用于启用扩展正则表达式,这样我们就可以使用更加灵活的正则表达式语法。**</span><br><span class="line">命令（常用）：</span><br><span class="line">[行号]a[内容]：新增，在行号后新增一行相应内容。行号可以是“数字”，在这一行之后新增，</span><br><span class="line"> 也可以是“起始行，终止行”，在其中的每一行后新增。当不写行号时，在每一</span><br><span class="line"> 行之后新增。使用$表示最后一行。后面的命令同理。</span><br><span class="line">[行号]c[内容]：取代。用内容取代相应行的文本。</span><br><span class="line">[行号]i[内容]：插入。在当前行的上面插入一行文本。</span><br><span class="line">[行号]d：删除当前行的内容。</span><br><span class="line">[行号]p：输出选择的内容。通常与选项-n一起使用。</span><br><span class="line">s/re（正则表达式）/string(/g)：将re匹配的内容替换为string /g表示全文替换，不加则替换第一个</span><br></pre></td></tr></table></figure>

<h3 id="常用用法"><a href="#常用用法" class="headerlink" title="常用用法"></a>常用用法</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">awk <span class="string">&#x27;$1&gt;2 &#123;print $1,$3&#125;&#x27;</span> my.txt</span><br><span class="line"><span class="comment"># awk &#x27;pattern action&#x27; file ，pattern 为条件，action 为命令，file 为文件</span></span><br><span class="line"><span class="comment"># 命令中出项的 $n 代表每一行中用空格分隔后的第 n 项</span></span><br><span class="line"><span class="comment"># 该命令的意义是文件 my.txt 中所有第一项大于 2 的行，输出第一项和第三项</span></span><br><span class="line"></span><br><span class="line">awk -F, <span class="string">&#x27;&#123;print $2&#125;&#x27;</span> my.txt</span><br><span class="line"><span class="comment"># -F 选项用来指定用于分隔的字符，默认是空格</span></span><br><span class="line"><span class="comment"># 该命令的意义是打印用 , 分隔后的第 2 项</span></span><br><span class="line"></span><br><span class="line">grep -n <span class="string">&quot;all&quot;</span> my.txt &gt; result.txt</span><br><span class="line"><span class="comment"># 打印含有“all”的行并显示行号</span></span><br><span class="line"></span><br><span class="line">sed -i <span class="string">&quot;s/<span class="variable">$2</span>/<span class="variable">$3</span>/g&quot;</span> <span class="variable">$1</span></span><br><span class="line"><span class="comment"># 将传入的第一个参数（文件）里的所有 第二个参数 替代为 第三个参数</span></span><br></pre></td></tr></table></figure>

<h2 id="shell编程注意"><a href="#shell编程注意" class="headerlink" title="shell编程注意"></a>shell编程注意</h2><ul>
<li><p>首句<code>#!/bin/bash</code></p>
</li>
<li><p>等号与运算符两边不允许有空格，<code>-op</code>的两边与<code>[]</code>内部两边必须有空格</p>
</li>
<li><p>获取变量值建议使用  <code>$&#123;var&#125;</code>，花括号避免歧义</p>
</li>
<li><p>引用变量必须在<strong>双引号</strong>中进行</p>
</li>
<li><p><code>$#</code> 传递的参数个数；<code>$*</code> 一个字符串，内容是传递的全部参数</p>
</li>
<li><p><code>let</code> 赋值运算 eg <code>let i=i+1</code> 等价于 <code>i=$(($i+1))</code>  （双括号是计算值）</p>
</li>
<li><p>流程控制</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> [ 表达式1 ]; <span class="keyword">then</span></span><br><span class="line">	命令列表1;</span><br><span class="line"><span class="keyword">elif</span> [ 表达式2 ]; <span class="keyword">then</span></span><br><span class="line">	命令列表2;</span><br><span class="line"><span class="keyword">else</span> [ 表达式3 ]; <span class="keyword">then</span></span><br><span class="line">	命令列表3;</span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> [ 表达式 ];<span class="keyword">do</span></span><br><span class="line">	命令列表;</span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> name <span class="keyword">in</span> [参数列表];<span class="keyword">do</span></span><br><span class="line">	命令列表;</span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"><span class="comment">#最后都有done</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h1 id="Lab0踩坑"><a href="#Lab0踩坑" class="headerlink" title="Lab0踩坑"></a>Lab0踩坑</h1><p><strong>在评测前先确保自己对于文件的变更已经全部添加到暂存区</strong>（使用合适的 <code>git add</code> 和 <code>git commit</code> ）！！<strong>注意先<code>add</code>再<code>commit</code></strong> ，新建的文件需要先添加跟踪才会到暂存区，才能用<code>commit</code>提交。</p>
<ul>
<li><p>整型转字符串 <code>sprintf</code></p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>,&amp;n);</span><br><span class="line"><span class="type">char</span> str[<span class="number">20</span>];</span><br><span class="line"><span class="built_in">sprintf</span>(str,<span class="string">&quot;%d&quot;</span>,n);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>,str)</span><br></pre></td></tr></table></figure>
</li>
<li><p>三剑客不熟悉</p>
</li>
<li><p>变量使用忘了</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">自增操作！</span><br><span class="line"><span class="comment">#错误写法</span></span><br><span class="line">a=a+1</span><br><span class="line"><span class="comment">#正确写法</span></span><br><span class="line">a=$[<span class="variable">$a</span>+1]</span><br></pre></td></tr></table></figure>
</li>
<li><p>在sed命令中</p>
<ul>
<li><p><strong>使用变量</strong>时，<strong>正则用双引号</strong>（一般只能用单引号）</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line">name=tomas</span><br><span class="line">sed -i <span class="string">&quot;s/rose/<span class="variable">$&#123;name&#125;</span>/g&quot;</span> name.txt</span><br></pre></td></tr></table></figure>
<ul>
<li><p>单引号<code>‘’</code></p>
<ul>
<li>括起来的字符均作为普通字符出现</li>
</ul>
</li>
<li><p>双引号<code>“”</code></p>
<ul>
<li>括起来的字符除 <code>$</code>、<code>\</code>、<code>‘’</code>、和 <code>“”</code> 之外都将作为普通字符对待，所以如果要用变量值就要用<code>“”</code></li>
</ul>
</li>
<li><p>倒引号&#96;&#96;&#96;&#96;</p>
<p>  用于命令替换，其所括字符串在被Shell解释时，首先执行其中的命令并将其结果代替该命令</p>
</li>
<li><p>$俩小括号&#x2F;$中括号<strong>计算结果</strong>替换，$单小括号<strong>执行结果</strong>替代</p>
</li>
</ul>
</li>
<li><p>要更改源文件内容要加参数<code>-i</code></p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 替换字符串，并更改原文件内容</span></span><br><span class="line"><span class="comment"># 在sed后面加 -i,即编辑文档“edit files in place”选项</span></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>gcc编译时如果头文件不在<code>/usr/include</code>里，一定记得<code>-I &lt;头文件所在位置路径&gt;</code></p>
</li>
</ul>
<h1 id="难点分析"><a href="#难点分析" class="headerlink" title="难点分析"></a>难点分析</h1><h3 id="Linux下命令种类多，参数复杂"><a href="#Linux下命令种类多，参数复杂" class="headerlink" title="Linux下命令种类多，参数复杂"></a>Linux下命令种类多，参数复杂</h3><ul>
<li><code>ls</code>、<code>mv</code>、<code>touch</code>等常用命令倒是能熟练掌握，但像<code>sed</code>、<code>awk</code>等参数复杂功能复杂的命令，在使用时就容易一下想不起来正确的使用方法</li>
<li>解决方法：先通过多练习熟练，使用时若不确定先查阅资料或参考书</li>
</ul>
<h3 id="shell编程中符号和空格使用非常容易出错"><a href="#shell编程中符号和空格使用非常容易出错" class="headerlink" title="shell编程中符号和空格使用非常容易出错"></a>shell编程中符号和空格使用非常容易出错</h3><p>比如</p>
<ul>
<li><p>各种引号的使用</p>
<ul>
<li><p>单引号<code>‘’</code></p>
<p>  括起来的字符均作为普通字符出现</p>
</li>
<li><p>双引号<code>“”</code></p>
<p>  括起来的字符除 <code>$</code>、<code>\</code>、<code>‘’</code>、和 <code>“”</code> 之外都将作为普通字符对待，所以如果要用变量值就要用<code>“”</code> </p>
</li>
<li><p>倒引号&#96;&#96;&#96;&#96;</p>
<p>  用于命令替换，其所括字符串在被Shell解释时，首先执行其中的命令并将其结果代替该命令</p>
</li>
</ul>
</li>
<li><p>各种括号的使用</p>
<ul>
<li><code>$(())</code>&#x2F;<code>$[]</code><strong>计算结果</strong>替换，<code>$()</code><strong>执行结果</strong>替代</li>
</ul>
</li>
<li><p>空格的使用</p>
<ul>
<li>等号与运算符两边不允许有空格，<code>-op</code>的两边与<code>[]</code>内部两边必须有空格</li>
</ul>
</li>
</ul>
<h3 id="Makefile嵌套"><a href="#Makefile嵌套" class="headerlink" title="Makefile嵌套"></a>Makefile嵌套</h3><ul>
<li>makefile嵌套的写法有很多，用<code>$(Make) -C subdir</code> （<code>-C</code> 是跳转到指定路径再执行make操作） 或者 <code>cd subdir &amp;&amp; make</code> 都可以，需要特别注意不要用混了</li>
<li>用<code>$(Make)</code>这种操作时想要做make clean，是<code>$(MAKE) clean -C subdir</code></li>
<li><code>gcc</code>编译时如果依赖的库文件不在本文件夹下，要记得用<code>-I</code>进行链接</li>
<li>注意相对路径和绝对路径的使用要准确</li>
</ul>
<h1 id="Lab0体会"><a href="#Lab0体会" class="headerlink" title="Lab0体会"></a>Lab0体会</h1><h3 id="编写-Shell-脚本和-make-文件时熟练度至关重要"><a href="#编写-Shell-脚本和-make-文件时熟练度至关重要" class="headerlink" title="编写 Shell 脚本和 make 文件时熟练度至关重要"></a>编写 Shell 脚本和 make 文件时熟练度至关重要</h3><p>虽然上学期学习了系统编程，有了一定的编写 Shell 脚本和 make 文件的基础。但由于课后自主训练较少，加上假期疏于复习，导致对相关操作并不完全熟练。但Shell 脚本和 make 文件的编写是在Linux环境下顺利进行实验的基础，因此需要在课后多多练习，尽快熟悉相关操作，以免后续实验中因基础操作不熟练浪费时间</p>
<h3 id="debug时利用好讨论区-利用好git操作"><a href="#debug时利用好讨论区-利用好git操作" class="headerlink" title="debug时利用好讨论区 &amp; 利用好git操作"></a>debug时利用好讨论区 &amp; 利用好<code>git</code>操作</h3><p>Excecise1 测评错误后，一直以为是自己哪里写错，反复检查每个文件，提交了4、5次，折腾了快2个小时还是错误。后来用<code>git status</code>才发现是没有用<code>git add</code>对新建的文件添加跟踪，导致一直没能把这部分文件成功提交 😢</p>
<p>后来在看讨论区时，更是发现一篇精华帖的开头就用大写加粗强调了这个问题……</p>
<h1 id="exam前准备"><a href="#exam前准备" class="headerlink" title="exam前准备"></a>exam前准备</h1><h2 id="血泪教训"><a href="#血泪教训" class="headerlink" title="血泪教训"></a>血泪教训</h2><ul>
<li><p>在bash中，整个程序的返回值$?&#x3D;最后一条语句的返回值$?；且在评测机中一旦程序的返回值$?为1，它就认为这个程序报错了；</p>
<p>  故最好在每一个脚本文件末尾都手动写一个<code>exit 0</code></p>
<ul>
<li><code>$?</code>表示上一条语句的返回值；<code>$#</code>表示传参个数</li>
</ul>
</li>
</ul>
<h2 id="不熟操作"><a href="#不熟操作" class="headerlink" title="不熟操作"></a>不熟操作</h2><ul>
<li><p>输出hello.c中所有包含“os_hello”（区分大小写）的行的“os_hello”（区分大小写）以左的内容，如果一行出现多个，则输出第一次出现以左的部分</p>
<ul>
<li><p><code>grep</code> &amp; <code>sed</code></p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep -E <span class="string">&#x27;.*os_hello&#x27;</span> hello.c | sed -E <span class="string">&#x27;s/(.*)os_hello.*/\1/&#x27;</span></span><br></pre></td></tr></table></figure>
<p>  正则表达式解释见<a href="https://www.notion.so/3799c59c1fbb431e92c71883c31aef6d?pvs=21">分组与捕获</a></p>
</li>
<li><p><code>awk</code> （两种均可）</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">awk -F <span class="string">&#x27;os_hello&#x27;</span> <span class="string">&#x27;&#123;print $1&#125;&#x27;</span> hello.c</span><br><span class="line">awk <span class="string">&#x27;/os_hello/ &#123; sub(/os_hello.*/, &quot;&quot;); print &#125;&#x27;</span> hello.c</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><code>#var</code> 是获取变量的长度，<code>$&#123;#var&#125;</code>获取长度值</p>
</li>
<li><p><strong>字符串切片操作</strong></p>
<p>  <code>$&#123;variable:start:count&#125;</code> 表示从变量 <code>variable</code> 的 <code>start</code> 位置开始，截取 <code>count</code> 个字符</p>
</li>
</ul>
<h2 id="坑"><a href="#坑" class="headerlink" title="坑"></a>坑</h2><ul>
<li>多层<code>if</code>容易忘记闭合<code>fi</code></li>
<li><code>&gt;&gt;</code> 追加会自动换行，想要不换行<code>echo-n xx &gt;&gt; xx</code></li>
<li><code>$?</code>返回值为 0 表示上一条命令执行成功，1 表示执行失败</li>
</ul>
]]></content>
      <categories>
        <category>OS</category>
      </categories>
      <tags>
        <tag>Lab</tag>
      </tags>
  </entry>
</search>
